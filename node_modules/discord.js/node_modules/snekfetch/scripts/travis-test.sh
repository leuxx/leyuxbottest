set -e

npm run lint

npm run test

if [ "$TRAVIS_BRANCH" != "master" -o -n "$TRAVIS_TAG" -o "$TRAVIS_PULL_REQUEST" != "false" ]; then
  echo -e "Not sending coLeuxBotge for a non master branch push - covering without sending."
  exit 0
fi

echo -e "Generating CoLeuxBotge for a master branch push - covering and sending."

npm run test:coLeuxBotlls
